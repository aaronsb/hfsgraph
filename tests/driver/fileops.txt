# Staged file ops (#38) on the zoom_inside leaf (5 files: f0.h f1.toml f2.o f3.cpp f4.md
# in List rows at x≈15, y≈222+14i). Rename f0.h → g0.h: the projection re-lists, the
# glyph at the same row now reads g0.h. A rename onto an existing name is refused and
# stages nothing. Trash f4.md: the directory's weight drops with it and the map
# re-flows (an ADR question: should a staged trash change the area before commit?).
resize 1200 800
load $HFSGRAPH_FIXTURE 2
wait
zoom 4 600 400
sleep 200
zoom 4 600 400
wait
sleep 200
probe 15 222
stage rename 15 222 g0.h
settle
check ops 1
probe 15 222
click 15 222
check file g0.h
stage rename 15 236 g0.h      # f1.toml → g0.h collides: refused, nothing staged
check ops 1
stage trash 15 278
settle
check ops 2
probe 15 278                  # the map re-flowed: the directory lost weight with the file
stage move @f1.toml 600 100   # by name (geometry-free): into the directory cell up there
settle
check ops 3
stage trash @f1.toml          # found in its new directory (the projection moved it)
check ops 4
shot driver_fileops.png view
echo fileops ok
quit
